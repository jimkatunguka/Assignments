package march2017;

import static org.junit.jupiter.api.Assertions.*;

import java.util.Arrays;
import java.util.List;

import org.junit.jupiter.api.Test;

class AdminTest {

	/*
	 * Student std1 = new Student("Joe", 3.2, Majors.CS); Student std2 = new
	 * Student("Tom", 3.5, Majors.CH); Student std3 = new Student("HeLing", 2.9,
	 * Majors.CS); Student std4 = new Student("Pierre", 2.8, Majors.ENG); Student
	 * std5 = new Student("Ihaku", 3.9, Majors.CS); Student std6 = new
	 * Student("Richard", 2.8, Majors.CS);
	 * 
	 * List<Student> list = Arrays.asList(std1, std2, std3, std4, std5, std6);
	 * 
	 * 
	 * @Test void test() { List<Student> expected = Arrays.asList(std1, std5);
	 * List<Student> actual = Admin.obtainHonorRoll(list); assertEquals(expected,
	 * actual); }
	 */

}
